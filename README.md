## Download

# Pip

```cmd
pip install aiotool
```

# Poetry

```cmd
poetry add aiotool
```


Feautures `Depend`:

Depend without response for Router, handler
Exit from depend before call handler
use_cache