[project]
name = "aiogram-tool"
version = "1.5.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiogram==3.29.1",
    "redis>=8.0.0",
]

[dependency-groups]
dev = [
    "aiohttp-socks>=0.11.0",
    "pytest-asyncio>=1.4.0",
]
