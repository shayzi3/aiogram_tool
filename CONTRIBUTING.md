# TypeError исправить .
# Написать ScopeRegistry .
# Подумать над инвалидацией .
# Приватные API Python .

# ContextDecorator .

# Immutable ScopeObject .
# Защитить Singleton от гонки .

# lru_cache для signature, убрать SignatureRegistry .
# _MISSING, Transient .
# *args и **kwargs .
# Убрать contextmanager .


# Протестить ошибку в зависимости
# Проверить добавляет ли aiogram event в middleware data